--Dataset: introduction-to-data-analytics From Data.World
--Source: https://data.world/jerrys/introduction-to-data-analytics
--Queried using: Data world notebook

--Mostre-me todos os personagens (characters) da Família Stark por ordem crescente de nome (character_name) 
-- Tabela got_characters

SELECT 
A.character_name
FROM got_characters A 
WHERE 0=0
AND A.character_name LIKE '%Stark%'
ORDER BY A.character_name
